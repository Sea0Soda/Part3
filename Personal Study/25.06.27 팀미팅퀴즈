# React State 배치 처리 문제

## 문제
다음 React 컴포넌트 코드를 보고 질문에 답하세요:

```jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    setCount(count + 1);
    setCount(count + 1);
    setCount(count + 1);
  };

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={handleClick}>+3 증가</button>
    </div>
  );
}